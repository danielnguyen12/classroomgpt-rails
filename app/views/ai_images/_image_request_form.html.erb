<%= simple_form_for @ai_image do |f| %>
  <div class="form-group">
    <%= f.text_area :prompt, class:'form-control' %>
  </div>
  <%= f.submit "Submit", class:'btn btn-outline-secondary' %>
<% end %>